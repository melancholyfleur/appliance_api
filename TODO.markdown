# Implement a pie chart that shows percentage of target addresses that are reachable.

## Ping backend part

## Viz frontend part

Make an API for the data... send via javascript... send JSON...

GET request to `/ping` or `/ping/:address`

